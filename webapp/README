Setup:
virtualenv venv -p $(which python3) 
source ./venv/bin/activate
pip3 install --upgrade pip
pip install -r Flask

Run web app (replace xxxx with a unique port):
source ./venv/bin/activate
export FLASK_APP=app
flask run -h 0.0.0 -p xxxx --reload
