In a terminal, cd into the DGEAP directory and open an R session. Then run:
install.packages("shiny")

Now run the demo by entering the following commands in the R session:
library(shiny)
runApp("shiny_demo")
